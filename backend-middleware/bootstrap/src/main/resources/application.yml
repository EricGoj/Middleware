spring:
  application:
    name: middleware
  
  datasource:
    url: jdbc:postgresql://localhost:5432/middleware
    username: middleware
    password: middleware
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
  
  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: UTC

server:
  port: 8080

logging:
  level:
    com.acme.middleware: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: true

# Jira configuration (Basic Auth)
jira:
  baseUrl: https://ericqdev7.atlassian.net
  email: ericqdev7@gmail.com
  apiToken: ""
  projectKey: MELICHALL
  # Optional shared secret to validate incoming webhooks (sent in 'x-webhook-secret' header)
  webhookSecret: ""

---
spring:
  config:
    activate:
      on-profile: test
  
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
  flyway:
    enabled: false

---
spring:
  config:
    activate:
      on-profile: local
  
  jpa:
    show-sql: true
  
logging:
  level:
    com.acme.middleware: DEBUG